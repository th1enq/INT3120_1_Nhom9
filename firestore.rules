rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // SIMPLIFIED RULE: Allow all authenticated users to access all documents
    // This covers all collections and subcollections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
